# Product Service Configuration
server.port=8082

# MongoDB Configuration - supports both local and Docker environments
# Note: Username and password come from Vault secrets
spring.data.mongodb.host=${MONGODB_HOST:localhost}
spring.data.mongodb.port=${MONGODB_PORT:27017}
spring.data.mongodb.database=${MONGODB_DATABASE:product-service}
spring.data.mongodb.authentication-database=${MONGODB_AUTH_DB:admin}

# Legacy test property
test.name=SUCCESS-refresh-working!